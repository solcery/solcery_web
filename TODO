TODO:




users добавляется в проект в отельную DB
singleton, projectSettings
базовый проект
сессия кладется в юзера

objects => 
escape for object window, hotkey provider


миграция поднимает новый проект и показывает на нем
field validators, required for name, non empty for bricks
аддитивный поиск для строчек
В фильтре енама писать не цифру
api logs
create new project
merge keys for map and arrays
caching template links on update in Link memo
object savedata check
content check
field descriptions
копипаст для филдов
enum -> link
sealed and readonly templates and fields
Доступы
таргеты констракта для полей
Валидация на сохранении
Переименовать fields в data
Системные поля id и enabled
Реактивное изменение и загрузка когда меняет другой
Конфиг
Топменю настраивается в монге (или вообще в контенте?)
смержить map и array
Нормальный рендер мап и эррэй

