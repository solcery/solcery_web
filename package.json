{
  "name": "sage",
  "version": "0.1.0",
  "description": "Solcery toolset",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "cd client/ && yarn start",
    "debug": "yarn build & yarn start",
    "build": "cd client/ && yarn && yarn build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.16.4",
    "mongoose": "^6.2.10",
    "react-router-dom": "^6.3.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/solcery/sage.git"
  }
}
